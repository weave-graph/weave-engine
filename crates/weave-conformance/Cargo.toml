[package]
name = "weave-conformance"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = "Fixed portable semantic conformance vectors, not an application runtime"
[lib]
crate-type = ["cdylib", "rlib"]
[dependencies]
serde_json = "1"
weave-contract = { path = "../weave-contract" }
weave-cluster = { path = "../weave-cluster" }
weave-spaces = { path = "../weave-spaces" }
