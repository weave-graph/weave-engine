# Governed effects and typed-vector checkpoint

Protocol 0.18 remains unchanged; SQLite store advances to 17. Native freeze is `8270096e2c47befc3d1622ebe709bdf984a59db8`, paired with source `2a02953901d30e7b0e0bce8b4abd5126fbaa0ad6`. This checkpoint does not close full paper or release acceptance.

The engine owner passed 418 all-feature workspace checks. Final bounded stored-response hardening and its added regression passed all 12 focused bridge tests, strict workspace all-target/all-feature Clippy, formatting and rebuilt fixture execution. The orchestrator independently passed three fault/authority tests: forged but rehashed closure removal, deletion of genuine empty input, and reconciliation after authority loss. They exercise actual persisted state and trusted grant installation.

The orchestrator independently executed actual source compilation through pure handler, governance acceptance, effect intent and an independent local SQLite reference sink: one compiler plus 36 native processes, including deliberate process deaths and lost responses. Idempotent destinations perform one action; non-idempotent uncertain outcomes stay unknown and cannot be canceled or redispatched. The report is `measurements/2026-09-20-root-governed-effects-source.json`. The owner native-template report has zero compiler processes and is separately labeled.

Populated stores 14 and 15 both upgraded to 17 with before/after-commit death, unchanged historical sealed views and signed receipts, exact replay and old-binary refusal. A real store16 compiler/handler fixture also passed eight migration checks: pending and completed preparations retain their identities and bytes, historical completion replays, and a pending command completes exactly once after upgrade. Reports are the dated `root-store14-to17`, `root-store15-to17` and `root-handler-store17-migration` JSON files. Hosted CI reproduces these from pinned public historical revisions.

Source typed vectors passed 164 tests, strict lint/format, 20,050-byte executed native/WASM parity, unchanged artifacts for 24 prior examples and a locked offline committed-source archive build. The orchestrator independently checked nominal space/revision/role boundaries, finite numbers and negative zero, duplicate escaped descriptor keys, exact aliases, and actual native geometry/privacy/time behavior. Its canonical pure `weave-spaces` validator is separately pinned to `2127b5aff5783a722537704064e61f5663650c8d`; protocol contract vendoring is unchanged. Integration CI runs the actual vector and Interval compilers against this runtime.

Trusted effect grants are native host configuration, bound to a same-principal governed publication and destination. They do not authenticate compiler origin, grant declassification, provide arbitrary source effects, or make a production network destination safe. Runtime isolation, retention/GC, richer lifecycle semantics, full persistent browser/mobile scenarios and the remaining requirement matrix stay open. Hosted success must be verified separately from these local results.
