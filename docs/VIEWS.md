# Durable named live views

`register_view` accepts an immutable `ViewDefinition` with a standalone graph expression and exact trusted principal. It persists the evaluated graph, selected snapshots, schema, provenance groups, coverage and head dependencies. Another principal receives a generic unavailable response; cache lookup never falls back to another visibility class. Program-local graph names must be expanded before registering a standalone expression.

`read_view(..., RequireCurrent, ...)` checks selected live heads in one SQLite snapshot and returns `E_FRESHNESS` when recomputation is required. `AllowStale` explicitly returns the old value with `current=false`. Unresolved partial coverage conservatively cannot certify cached freshness. `refresh_view` reevaluates the pure expression and atomically persists its result, dependency list, generation and membership transition. Explicitly enrolled proof-free Query/Filter views can use [incremental membership maintenance](INCREMENTAL_SELECTION.md). Full recomputation remains the correctness oracle/fallback; full-snapshot I/O and output repinning remain linear.

Live metadata handles participate in dependency tracking. Pinned metadata remains pinned when its graph head changes. A reverse dependency table indexes graph/branch subscriptions for [bounded durable scheduling](VIEW_SCHEDULING.md). The host explicitly scans notifications and drains work; there is no background thread or push delivery guarantee.

Fixed-clock views preserve the expression's time selectors and reject a supplied tick. Tick-clock views require an explicit instant and substitute all query/filter/support time selectors with that instant. Moving a registered tick backwards rejects; a distinct historical view can be registered. Expiry can retract an edge without producing a source graph commit. No hidden timer or implicit wall-clock fact filter changes the pure expression. Current authorization separately uses the [trusted operation clock](OPERATION_CLOCK.md), including cached reads; advancing a fact-time tick cannot backdate authority.

`view_changes` reports added, removed and changed node/edge IDs plus the complete new result. Removal is view membership, not an assertion of falsity. Partial/unavailable inputs remain explicit in the result. Schema, metadata and coverage changes are carried by the full result even if node/edge membership does not change. Only the latest transition is retained; a consumer more than one generation behind receives `E_REPLAY_WINDOW` and must resynchronize from a snapshot.

Native restart, deletion/correction, live metadata, expiry and full-recomputation comparisons are tested. Remaining E07 work includes efficient incremental operators, richer priority/fan-out scheduling and durable ticking adapters, event-driven push subscriptions, configurable history/retention quotas, and richer versioned policy contexts. Stored view values and transitions revalidate source proof restrictions and current native identity policies before release, including AllowStale. A transition retains a bounded prior-result authorization guard because removed IDs can disclose old restricted objects even when the new graph is empty. Revoked transitions return generic unavailable; the caller can resynchronize from an authorized refreshed snapshot. Older transitions without this guard require snapshot resynchronization. This does not add remote capability administration to the native view API.

Persisted view loads enforce byte limits inside SQLite before allocating or parsing definition/result/dependency JSON: 1 MiB definition, 32 MiB result, 4 MiB dependency vector and the remaining shared operation budget. All three fields share one combined-size guard; read counts and bytes are charged. Duplicate registration also bounds its stored definition read. Nonpositive stored generations and oversized dependency counts reject. A corruption/restore regression exercises stale read, refresh and repeated registration, then verifies that rejected reads do not alter the retained generation. These are stored-text bounds, not SQLite I/O or process-RSS isolation.

Protocol0.16 adds separate `register_compiled_view(instance_id, template, tick, host)` installation and `read_current_view(selection, host)` access. Validated artifacts contain a canonical definition digest and complete source manifest; marker14 stores a redundant private binding and refuses legacy-row impersonation. Definition identity and source revisions enter every initial/full/fallback/incremental result and scheduler definition fingerprint. See [canonical artifact and replay boundary](contract/v0.16/README.md). Both AcceptedGraph and CurrentView expressions are recursively rejected in any registered recipe until dynamic acceptance/view dependencies are implemented. The first compiled recipe profile is live Query/Filter without metadata expansion. Source compilation alone installs nothing.
